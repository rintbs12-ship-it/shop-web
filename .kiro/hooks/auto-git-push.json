{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto Git Push on File Save",
      "trigger": "PostFileSave",
      "description": "Auto git add, commit, and push to GitHub every time a file is saved — triggers Railway auto deploy",
      "matcher": "\\.(html|css|js|json)$",
      "action": {
        "type": "command",
        "command": "cd \"e:\\Tool Reg Account\" && git add -A && git diff --cached --quiet || git commit -m \"auto: update $(date +%Y-%m-%d %H:%M:%S)\" && git push"
      }
    }
  ]
}